<Window x:Class="TaskManagerApp.Views.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerApp.Views"
        mc:Ignorable="d"
        Title="ForgotPasswordWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Логин:" Margin="0,5,5,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="LoginTextBox" Margin="0,5,0,5"/>

        <Button Grid.Row="1" Grid.Column="1" Content="Восстановить" Click="ResetPasswordButton_Click" Margin="0,10,0,0" HorizontalAlignment="Right" Width="140"/>
    </Grid>
</Window>
