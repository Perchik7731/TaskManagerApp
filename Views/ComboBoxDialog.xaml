<Window x:Class="TaskManagerApp.Views.ComboBoxDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerApp.Views"
        mc:Ignorable="d"
        Title="ComboBoxDialog" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="TitleLabel"  Grid.Row="0" Content="Выберите значение:" Margin="0,5,5,5"/>
        <ComboBox x:Name="ItemsComboBox" Grid.Row="1" Margin="0,5,0,5"/>
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal" >
            <Button Content="Выбрать" Click="SelectButton_Click" Margin="0,10,5,0" Width="80"/>
            <Button Content="Отмена" Click="CancelButton_Click" Margin="0,10,0,0" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
